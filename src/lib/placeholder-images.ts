// This file defines the type for an image placeholder.
// The actual data is now sourced from placeholder-images.json

export type ImagePlaceholder = {
  id: string;
  description: string;
  imageUrl: string;
  imageHint: string;
};
